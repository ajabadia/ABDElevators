# QUICK START (5 MINUTOS)
## Para comenzar YA

---

## 🎯 OBJETIVO

Después de 5 minutos:
- ✅ Entiendes las 10 reglas (visión general)
- ✅ Sabes dónde está todo (docs)
- ✅ Tienes acciones para HOY

---

## 📋 LAS 10 REGLAS EN 1 MINUTO

```
1. TypeScript STRICT (noImplicitAny: true)
2. Zod BEFORE processing (Schema.parse() primero)
3. AppError (no Error())
4. Logging estructurado (logEvento con correlacion_id)
5. NO localStorage/sessionStorage
6. Validación cliente + servidor
7. DB transactions (múltiples ops)
8. Performance medida (log tiempo)
9. Security headers (CORS whitelist, rate limit)
10. NO secrets hardcodeados (process.env)
```

---

## 🚫 RED FLAGS = RECHAZAR PR

Si ves estos → rechaza sin preguntar:

```
❌ const x: any
❌ console.log('API Key', key)
❌ logEvento(...) sin await
❌ .find({}).toArray() sin .limit()
❌ localStorage / sessionStorage
```

---

## 📚 DOCUMENTOS (Elige por Rol)

| Rol | Hacer HOY | Archivos |
|-----|----------|----------|
| **Developer** | Leer 5 min + bookmark | `reference-card-1-page.md` |
| | Setup Cursor rules | `instrucciones-cursor-antigrávity.md` |
| | Antes de commit | `checklist-pre-commit-ejecutable.md` |
| **Code Reviewer** | Leer completo | `reglas-de-oro-desarrollo-ia.md` |
| | Usar checklist | `checklist-pre-commit-ejecutable.md` |
| **IA** | Copy en rules | `instrucciones-cursor-antigrávity.md` |
| **Manager** | Overview | `resumen-ejecutivo-stakeholders.md` |

---

## ⚡ 3 ACCIONES PARA HOY

### 1️⃣ Setup Cursor Rules (2 min)

```bash
# Opción A: Copiar archivo
cp instrucciones-cursor-antigrávity.md .cursor/rules

# Opción B: Manual
# Abre .cursor/rules en Cursor
# Copia contenido de instrucciones-cursor-antigrávity.md
```

### 2️⃣ Setup Pre-Commit Script (2 min)

```bash
# package.json - añadir:
"scripts": {
  "pre-commit": "npm run lint && npm run type-check && npm run test && npm run build"
}

# Luego, antes de cada push:
npm run pre-commit
```

### 3️⃣ Print Reference Card (1 min)

```bash
# Imprimir reference-card-1-page.md
# PIN EN TU MONITOR

# Bookmarks:
- RED FLAGS (top)
- 10 Reglas (tabla)
- Template endpoint
```

---

## 🎬 PRIMER ENDPOINT (HOY)

Usar como template:

```typescript
// app/api/[feature]/[action]/route.ts

import { NextRequest, NextResponse } from 'next/server'
import { z } from 'zod'
import { logEvento } from '@/lib/logger'
import { generateUUID } from '@/lib/utils'

// 1. SCHEMA (Zod validation)
const InputSchema = z.object({
  // fields
})

export async function POST(request: NextRequest) {
  const correlacion_id = generateUUID()
  
  // 2. LOG START
  await logEvento({
    nivel: 'INFO',
    origen: 'API_FEATURE',
    accion: 'ACTION',
    mensaje: 'Starting',
    correlacion_id
  })

  try {
    // 3. VALIDATE
    const data = InputSchema.parse(await request.json())

    // 4. PROCESS
    const result = await process(data)

    // 5. LOG SUCCESS
    await logEvento({
      nivel: 'INFO',
      origen: 'API_FEATURE',
      accion: 'ACTION',
      mensaje: 'Success',
      correlacion_id,
      detalles: { result_size: JSON.stringify(result).length }
    })

    return NextResponse.json({ success: true, data: result })

  } catch (error) {
    // 6. LOG ERROR
    await logEvento({
      nivel: 'ERROR',
      origen: 'API_FEATURE',
      accion: 'ACTION',
      mensaje: error instanceof Error ? error.message : 'Unknown',
      correlacion_id,
      stack: error instanceof Error ? error.stack : undefined
    })

    // 7. RETURN ERROR
    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { success: false, code: 'VALIDATION_ERROR', message: error.errors[0].message },
        { status: 400 }
      )
    }

    return NextResponse.json(
      { success: false, code: 'INTERNAL_ERROR', message: 'Failed' },
      { status: 500 }
    )
  }
}
```

---

## ✅ CHECKLIST HOY

```
ANTES DE DORMIR ESTA NOCHE:

Lectura:
☑️ Leí reference-card-1-page.md (5 min)
☑️ Entiendo las 10 reglas
☑️ Sé qué son RED FLAGS

Setup:
☑️ Copié instrucciones-cursor-antigrávity.md → .cursor/rules
☑️ Configuré npm run pre-commit
☑️ Imprimí reference-card

Tomorrow:
☑️ Voy a escribir código respetando reglas
☑️ Ejecutaré npm run pre-commit antes de push
☑️ Si falla, consultaré reference-card o casos-uso-reales
```

---

## 🆘 AYUDA RÁPIDA

### "¿Cómo valido un input?"
→ Ver `casos-uso-reales-ejemplos.md` CASO 1

### "¿Cómo llamo a Gemini con retry?"
→ Ver `casos-uso-reales-ejemplos.md` CASO 2

### "¿Cómo hago transaction en MongoDB?"
→ Ver `casos-uso-reales-ejemplos.md` CASO 3

### "¿Por qué me rechazaron el PR?"
→ Busca en `reference-card-1-page.md` RED FLAGS

### "¿Necesito completitud en Regla #X?"
→ Ver `reglas-de-oro-desarrollo-ia.md` sección correspondiente

---

## 🚀 MAÑANA

Cuando generes código con Cursor:

```
1. Abre Cursor
2. Pega en el chat (o ya está en .cursor/rules):
   instrucciones-cursor-antigrávity.md
3. Describe qué necesitas
4. IA genera respetando todas las reglas
5. Ejecuta: npm run pre-commit
6. Si TODO green ✅ → git push
7. PR review = checklist automático (rápido)
```

---

## 📞 PREGUNTAS?

| Pregunta | Respuesta |
|----------|-----------|
| ¿Dónde está TODO? | `INDEX-central-documentos.md` |
| ¿Ejemplo de endpoint? | `casos-uso-reales-ejemplos.md` |
| ¿Todas las reglas? | `reglas-de-oro-desarrollo-ia.md` |
| ¿Cómo usar IA? | `instrucciones-cursor-antigrávity.md` |
| ¿Checklist pre-commit? | `checklist-pre-commit-ejecutable.md` |

---

## ✨ RESUMEN

**Hoy hiciste:**
- ✅ Entendiste las 10 reglas (1 min)
- ✅ Setup Cursor + pre-commit (2 min)
- ✅ Printaste reference card (1 min)
- ✅ Viste template de endpoint (1 min)

**Total: 5 minutos**

**Mañana:**
- ✅ Escribes código que pasa todas las pruebas
- ✅ 0 bugs en producción
- ✅ Logs completos para debugging
- ✅ Seguridad garantizada

**Resultado:**
- 🚀 MVP funcional en 4 semanas
- 🔒 Production-ready desde Día 1
- 📊 40-60 horas/sprint ahorradas
- 💰 32x ROI en 30 días

---

**QUICK START v1.0 | 21 Enero 2026 | ~5 minutos**

¡Listo para comenzar! 🚀
